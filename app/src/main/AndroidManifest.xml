<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tokko.recipes">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <permission
        android:name="com.example.gcm.permission.C2D_MESSAGE"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.example.gcm.permission.C2D_MESSAGE" />

    <application
        android:name=".RecipeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".genericlists.GenericListActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Holo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
        <activity
            android:name=".abstractlistdetailedits.AbstractDetailActivity"
            android:label="@string/title_recipe_detail"
            android:parentActivityName=".abstractlistdetailedits.GenericListActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".abstractlistdetailedits.GenericListActivity" />
        </activity>
        -->
        <meta-data
            android:name="roboguice.modules"
            android:value="com.tokko.recipes.utils.LoaderBindModule" />
    </application>

</manifest>